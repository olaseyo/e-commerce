import React from "react";
import Authentication from "../components/Authentication";

const LoginPage = () => {
  return (
    <div>
      <h1>Login Page</h1>
      <Authentication />
    </div>
  );
};

export default LoginPage;
